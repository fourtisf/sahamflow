# For local dev pointing directly at the backend:
#   NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
# In production behind Nginx, leave unset — the app uses the relative /api/v1
# path which Nginx proxies to the backend.
